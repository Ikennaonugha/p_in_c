#include <stdio.h>

int main(void) {
    // Print a welcome message to the terminal
    printf("Hello, C World!\n");

    // Print a basic math operation
    int a = 5;
    int b = 10;
    printf("The sum of %d and %d is %d\n", a, b, a + b);

    return 0;
}